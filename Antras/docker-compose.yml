version: '3'

services:
  web:
    build: .
    command: python app.py
    ports:
      - "80:5000"
    volumes:
      - .:/code

  paulius:
    build: ./PauliausService/
    command: python app.py
    ports:
      - "5001:5001"
    depends_on:
      - web
